angular.module('chatApp', [])
    .service('queueService', function () {
        var channelID;
        var userName;
        var userID;
        var queue = {
            setChannelID : setChannelID,
            
        };

    });